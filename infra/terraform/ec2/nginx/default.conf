server {
  listen 80;
  server_name _;

  location = /hello {
    add_header Content-Type text/plain;
    return 200 "hello\n";
  }

  location /api/ {
    proxy_pass http://backend:8080/;
  }

  location / {
    proxy_pass http://frontend:3000/;
  }
}
